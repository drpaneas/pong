package main

// Game constants (global)
const (
	screenWidth          = 1280
	screenHeight         = 720
	halfGameScreenWidth  = screenWidth / 2
	halfGameScreenHeight = screenHeight / 2
	pointsToWin          = 10
	maxEnemySpeed        = 12
)
